---
'@sanity-labs/react-table-kit': minor
---

Add controlled `serverGroup` support for server-backed grouping flows, including `groupValue` and `groupField` support on custom columns so grouped tables can use friendly labels while still grouping by backend fields or GROQ expressions.
